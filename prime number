// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int start,end,n,temp=0;
    printf("enter the range\n");
    scanf("%d %d",&start,&end);
    
    for(int j=start;j<=end;j++){
        n=j;
        for(int i=2;i<=n-1;i++){
            if(n%i==0){
                temp=1;
                break;
            }
        }
    
        if(temp==0)
            printf(" %d",n);
        else{
           // printf("\nIt is not a prime number %d",n);
            temp=0;
        }
        
   
            
    }
   
    return 0;
}
